## Imported Claude Cowork project instructions

In the folder we already created the codes. However, the results which is in the .tex file is not compatible with the paper. I am very fine to code everything from the scrach and remove old codes
The sumulation is based on the dependent noise, which is not what we build in our paper. 
Also the empirical part is something that needs to worked more and the results needs to be more explained. 
This project is genuinely two things wearing one name. Part of it is knowledge work: pulling the estimator spec out of the manuscript, interpreting what the runs produce, rewriting the simulation and empirical sections with real numbers, producing polished tables and figures, coordinating between the paper and the outputs. The other part is a reproducible software artifact — the package a Data Editor runs and that must reproduce every number on a clean machine.
Also In the other attached file (.md file), I give you my suggestion for implementation of this paper code.
Helping with that and keep that in mind I should make eventually a github repo and everything should be reproducible, so efficient code also matter to use all techniques to make it fast.
